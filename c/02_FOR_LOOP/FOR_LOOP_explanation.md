!! This file will not run in CCS, because the compiler will delete (optimise) all the code since it does not interact with real world (no obesrvable output). To run it, go to
> File -> Properties -> Optimisation -> Optimisation Level -> off

This file contains an example of a for loop that updates a variable with a counter.

Concepts:
- for loop
- value transfer between variables
