!! This file will not run in CCS, because the compiler will delete (optimise) all the code since it does not interact with real world (no observable output). To run it, go to
> File -> Properties -> Optimisation -> Optimisation Level -> off

This file contains an example of a switch statement. ```break``` prevents it from falling into the next statement, ```default``` is used as a fallback to handle other cases. 

Concepts covered:
- switch statement
- case selection
- boolean-like variable initialisation
