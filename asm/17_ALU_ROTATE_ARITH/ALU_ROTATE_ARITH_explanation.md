This file contains an example of rotate instructions ```rla``` (rotate left arithmetically) and ```rra``` (rotate right arithmetically). They shift the bits in ```dst``` either right or left and populate the vacated spots with 0 or 1. C flag is treated as an additional bit. An instruction ```clrc``` (clear C) is used to clear C flag.

Concepts covered:
- rotate arithmetically
- clear C instruction
