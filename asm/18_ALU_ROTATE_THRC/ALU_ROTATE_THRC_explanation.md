This file contains an example of rotate through carry. The ```rrc``` and ```rlc``` behave a lot like [Rotate arithmetically](/asm/17_ALU_ROTATE_ARITH), but they consider C flag as an extra bit.

Concept covered:
- rotate through carry
